---
description: Enable Aftertone local spoken TTS. Use only when the user runs /aftertone_on.
disable-model-invocation: true
---

## Result (activation already ran)

!`bash ~/.cursor/hooks/aftertone-activate.sh`

Show the user **only** the Result block above (you may trim to one short paragraph). Do **not** call Bash or other tools. Do **not** plan or edit files. If the block shows an install error, say to run the Aftertone install script. Otherwise note that speech needs `<spoken_summary>` on substantive replies and `/aftertone_off` turns it off.
